@model IEnumerable<BoVoyageFinalProject.Models.Insurance>

@{
}
    <p>
        @Html.ActionLink("Ajouter une nouvelle assurance", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InsuranceCost)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InsuranceType)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InsuranceCost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InsuranceType)
            </td>
            <td>
                @Html.ActionLink("Modifier", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Détails", "Details", new { id=item.ID }) |
                @Html.ActionLink("Supprimer", "Delete", new { id=item.ID })
            </td>
        </tr>
    }
    
    </table>
